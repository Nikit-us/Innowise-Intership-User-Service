spring:
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
  liquibase:
    enabled: false

  data:
    redis:
      port: 0
auth:
  service:
    url: 0
loki:
  endpoint: http://localhost:3100

management:
  otlp:
    tracing:
      endpoint: http://localhost:4317