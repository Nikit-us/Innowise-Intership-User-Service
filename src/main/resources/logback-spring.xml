<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <springProperty scope="context" name="appName" source="spring.application.name"/>
    <springProperty scope="context" name="lokiEndpoint" source="loki.endpoint"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [${appName},%X{traceId:-},%X{spanId:-}] %logger{36} - %msg%n
            </pattern>
        </encoder>
    </appender>
    <appender name="LOKI" class="com.github.loki4j.logback.Loki4jAppender">
        <http>
            <url>${lokiEndpoint}</url>
            <connectionTimeoutMs>2000</connectionTimeoutMs>
            <readTimeoutMs>2000</readTimeoutMs>
        </http>

        <format>
            <label>
                <pattern>app=${appName},host=${HOSTNAME},level=%level</pattern>
            </label>
            <message>
                <pattern>
                    {
                    "timestamp":"%d{yyyy-MM-dd HH:mm:ss.SSS}",
                    "level":"%level",
                    "app":"${appName}",
                    "traceId":"%X{traceId:-}",
                    "spanId":"%X{spanId:-}",
                    "thread":"%thread",
                    "logger":"%logger{36}",
                    "message":"%msg"
                    }
                </pattern>
            </message>
        </format>
    </appender>

    <appender name="ASYNC_LOKI" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="LOKI"/>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <springProfile name="!test">
            <appender-ref ref="ASYNC_LOKI"/>
        </springProfile>
    </root>
</configuration>